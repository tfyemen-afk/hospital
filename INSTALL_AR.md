# دليل تثبيت منصة المستشفى على XAMPP

## المتطلبات الأساسية

قبل البدء، تأكد من أن لديك:
- ✅ XAMPP مثبت على جهازك
- ✅ PHP 5.6 أو أحدث (يفضل PHP 7.3)
- ✅ MySQL 5 أو أحدث
- ✅ جميع امتدادات PHP المطلوبة مفعلة

## خطوات التثبيت

### الخطوة 1: تشغيل XAMPP

1. افتح **XAMPP Control Panel**
2. شغّل **Apache** (اضغط Start)
3. شغّل **MySQL** (اضغط Start)
4. تأكد من أن كلا الخدمتين تعملان (ستظهر باللون الأخضر)

### الخطوة 2: إنشاء قاعدة البيانات

1. افتح المتصفح واذهب إلى: `http://localhost/phpmyadmin`
2. انقر على **New** (جديد) في القائمة الجانبية
3. أدخل اسم قاعدة البيانات: `hospital_db` (أو أي اسم تفضله)
4. اختر **Collation**: `utf8_general_ci`
5. انقر على **Create** (إنشاء)

### الخطوة 3: التحقق من ملفات المشروع

تأكد من أن جميع الملفات موجودة في المجلد:
```
c:\xampp\htdocs\hospital\
```

### الخطوة 4: إعداد الصلاحيات (Permissions)

**ملاحظة:** على Windows، عادة لا تحتاج لتغيير الصلاحيات، لكن تأكد من أن المجلدات التالية قابلة للكتابة:
- `/mvc/libraries`
- `/mvc/config`
- `/uploads`

### الخطوة 5: تشغيل معالج التثبيت

1. افتح المتصفح واذهب إلى:
   ```
   http://localhost/hospital/index.php/install/index
   ```
   أو
   ```
   http://localhost/hospital/install/index
   ```

2. سترى صفحة **Check List** التي تتحقق من:
   - إصدار PHP
   - امتدادات PHP المطلوبة
   - صلاحيات المجلدات

3. إذا كانت جميع العناصر بخير، انقر على **Next Step**

### الخطوة 6: إدخال Purchase Code (إن وجد)

إذا طُلب منك إدخال Purchase Code، أدخله أو تخط هذه الخطوة إذا لم يكن مطلوباً.

### الخطوة 7: إعداد قاعدة البيانات

أدخل معلومات قاعدة البيانات:
- **Hostname**: `localhost`
- **Database**: اسم قاعدة البيانات التي أنشأتها (مثل: `hospital_db`)
- **Username**: `root` (الافتراضي في XAMPP)
- **Password**: اتركه فارغاً (أو أدخل كلمة المرور إذا قمت بتغييرها)

انقر على **Next Step**

### الخطوة 8: إعداد المنطقة الزمنية

اختر المنطقة الزمنية المناسبة لبلدك.

### الخطوة 9: إعداد معلومات الموقع

أدخل معلومات المستشفى:
- **Site Title**: اسم المستشفى
- **Phone**: رقم الهاتف
- **Email**: البريد الإلكتروني
- وغيرها من المعلومات المطلوبة

### الخطوة 10: إنشاء حساب المدير

أنشئ حساب المدير الرئيسي:
- **Username**: اسم المستخدم
- **Password**: كلمة المرور
- **Email**: البريد الإلكتروني

### الخطوة 11: إنهاء التثبيت

بعد إكمال جميع الخطوات، انقر على **Go to Login** للانتقال إلى صفحة تسجيل الدخول.

## الوصول إلى النظام

بعد التثبيت، يمكنك الوصول إلى النظام من:
```
http://localhost/hospital/
```

أو
```
http://localhost/hospital/index.php/signin
```

## استكشاف الأخطاء

### المشكلة: صفحة بيضاء
- تأكد من أن Apache يعمل
- تحقق من ملف `error_log` في XAMPP

### المشكلة: خطأ في الاتصال بقاعدة البيانات
- تأكد من أن MySQL يعمل
- تحقق من معلومات قاعدة البيانات
- تأكد من أن قاعدة البيانات موجودة

### المشكلة: خطأ في الصلاحيات
- على Windows، عادة لا تحتاج لتغيير الصلاحيات
- تأكد من أن المجلدات قابلة للكتابة

### المشكلة: mod_rewrite لا يعمل
- تأكد من تفعيل `mod_rewrite` في Apache
- تحقق من ملف `.htaccess` موجود

## ملاحظات مهمة

1. **البيئة**: النظام مضبوط على `development` في ملف `index.php`
2. **الأمان**: بعد التثبيت، غيّر `installed` إلى `true` في ملف `config.php`
3. **النسخ الاحتياطي**: احتفظ بنسخة احتياطية من قاعدة البيانات

## الدعم

إذا واجهت أي مشاكل، راجع ملف التوثيق في مجلد `documentation`.
