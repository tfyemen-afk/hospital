# 🔧 حل مشكلة الاتصال بـ MySQL

## 🔍 المشكلة:
```
No connection could be made because the target machine actively refused it
```

هذا يعني أن MySQL لا يعمل أو المنفذ غير صحيح.

## ✅ الحلول:

### الحل 1: تأكد من أن MySQL يعمل

1. **افتح XAMPP Control Panel**
2. **تحقق من MySQL:**
   - يجب أن يكون باللون الأخضر
   - إذا كان باللون الأحمر، انقر على **Start** بجانب MySQL
   - انتظر حتى يتحول للون الأخضر

### الحل 2: تحقق من المنفذ

MySQL في XAMPP يعمل عادة على المنفذ **3306**.

إذا كان Apache يعمل على 8080، جرب:

**في صفحة قاعدة البيانات:**
- **اسم المضيف:** `localhost:3306`
- أو: `127.0.0.1:3306`

### الحل 3: معلومات قاعدة البيانات الصحيحة

**لـ XAMPP:**
- **اسم المضيف:** `localhost` أو `127.0.0.1`
- **قاعدة البيانات:** `hospital` (أو الاسم الذي أنشأته)
- **اسم المستخدم:** `root`
- **كلمة المرور:** (اتركه فارغاً)

### الحل 4: إنشاء قاعدة البيانات

إذا لم تكن قاعدة البيانات موجودة:

1. **افتح phpMyAdmin:**
   ```
   http://localhost:8080/phpmyadmin
   ```

2. **أنشئ قاعدة بيانات جديدة:**
   - انقر على **New** في القائمة الجانبية
   - أدخل الاسم: `hospital`
   - اختر **Collation:** `utf8_general_ci`
   - انقر على **Create**

### الحل 5: إعادة تشغيل MySQL

1. **في XAMPP Control Panel:**
   - انقر على **Stop** بجانب MySQL
   - انتظر 5 ثوان
   - انقر على **Start** بجانب MySQL
   - انتظر حتى يتحول للون الأخضر

## 📋 خطوات التحقق:

1. ✅ MySQL يعمل في XAMPP (باللون الأخضر)
2. ✅ قاعدة البيانات `hospital` موجودة في phpMyAdmin
3. ✅ معلومات الاتصال صحيحة:
   - Hostname: `localhost`
   - Database: `hospital`
   - Username: `root`
   - Password: (فارغ)

## 🔍 إذا استمرت المشكلة:

1. **تحقق من ملف إعدادات MySQL:**
   - `C:\xampp\mysql\bin\my.ini`
   - تأكد من أن المنفذ 3306 مفعل

2. **تحقق من أن المنفذ 3306 غير مستخدم:**
   - افتح Command Prompt
   - اكتب: `netstat -an | findstr 3306`
   - يجب أن يظهر MySQL يستمع على المنفذ

3. **جرب الاتصال من سطر الأوامر:**
   ```
   cd C:\xampp\mysql\bin
   mysql -u root -p
   ```
   (اترك كلمة المرور فارغة واضغط Enter)

---

**ابدأ بالحل 1: تأكد من أن MySQL يعمل في XAMPP!** 🔄
