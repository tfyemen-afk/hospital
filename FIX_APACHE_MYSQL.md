# 🔧 حل مشاكل Apache و MySQL

## 🔍 المشاكل:

1. **Apache لا يعمل:** المنفذ 443 مستخدم من VMware
2. **قاعدة البيانات معلقة:** مشكلة في الاتصال

## ✅ الحلول:

### الحل 1: إصلاح مشكلة Apache (المنفذ 443)

**الخيار 1: تعطيل SSL في Apache (الأسهل)**

1. افتح ملف: `C:\xampp\apache\conf\httpd.conf`
2. ابحث عن: `Include conf/extra/httpd-ssl.conf`
3. أضف `#` في البداية لتعطيله:
   ```
   #Include conf/extra/httpd-ssl.conf
   ```
4. احفظ الملف
5. أعد تشغيل Apache

**الخيار 2: تغيير منفذ SSL**

المنفذ 4433 مضبوط بالفعل في `httpd-ssl.conf`، لكن Apache يحاول استخدام 443.

1. افتح ملف: `C:\xampp\apache\conf\httpd.conf`
2. ابحث عن: `Include conf/extra/httpd-ssl.conf`
3. أضف `#` في البداية لتعطيله (إذا لم تكن بحاجة لـ SSL)

### الحل 2: إصلاح مشكلة قاعدة البيانات

**الخطوات:**

1. **تأكد من أن MySQL يعمل:**
   - XAMPP Control Panel → MySQL يجب أن يكون باللون الأخضر ✅

2. **أنشئ قاعدة البيانات:**
   - افتح: `http://localhost:8080/phpmyadmin`
   - انقر على **New**
   - أدخل الاسم: `hospital`
   - اختر **Collation:** `utf8_general_ci`
   - انقر على **Create**

3. **أدخل المعلومات في صفحة التثبيت:**
   - اسم المضيف: `localhost`
   - قاعدة البيانات: `hospital`
   - اسم المستخدم: `root`
   - كلمة المرور: (فارغ)

4. **أعد تحميل الصفحة** (Ctrl+F5)

## 📋 خطوات سريعة:

1. ✅ تعطيل SSL في Apache (إذا لم تكن بحاجة له)
2. ✅ أعد تشغيل Apache
3. ✅ تأكد من أن MySQL يعمل
4. ✅ أنشئ قاعدة البيانات `hospital`
5. ✅ أدخل المعلومات في صفحة التثبيت

---

**ابدأ بالحل 1: تعطيل SSL في Apache!** 🔄
